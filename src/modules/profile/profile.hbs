<div class='{{styles.profile}}'>
  {{{avatar}}}

  <h1 class='visually-hidden'>Профиль пользователя</h1>
  <h2 class='{{styles.title}}'>{{user.first_name}}</h2>

  {{{userData}}}

  <div class='{{styles.links}}'>
    <a href='/edit/settings'>Изменить данные</a>
    <a href='/edit/password'>Изменить пароль</a>
    {{{logoutButton}}}
  </div>

  {{#if isVisiblePopup}}
    <div>
  {{else}}
    <div hidden>
  {{/if}}
   {{{avatarForm}}}
  </div>
</div>